services:
  analyzer:
    build: .
    ports:
      - "${PORT:-8000}:${PORT:-8000}"
    environment:
      HOST: 0.0.0.0
      PORT: ${PORT:-8000}
      MAX_UPLOAD_SIZE: ${MAX_UPLOAD_SIZE:-209715200}
      ANALYZER_TIMEOUT: ${ANALYZER_TIMEOUT:-180}
      MAX_ANALYSES: 8
